kvm vm in C without the use of libvirt or qemu.

Features:
- A vm.
- basic UART emulation for a shell to work.

What it does NOT have:
- Everything, it just loads into an initramfs shell.

i'm not putting this on my resume so if you're reading this then hello ig.
